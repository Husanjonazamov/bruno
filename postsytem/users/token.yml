info:
  name: token
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8034/auth/token/
  body:
    type: json
    data: |-
      {
        "phone": 9,
        "password": 9
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
