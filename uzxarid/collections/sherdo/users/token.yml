info:
  name: token
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/token/"
  body:
    type: json
    data: |-
      {
        "phone": 940014741,
        "password": 940014741
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
