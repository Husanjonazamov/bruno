info:
  name: getDetail
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/quick-evaluation/1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
