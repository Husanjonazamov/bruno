info:
  name: authToken
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/auth/token/"
  body:
    type: json
    data: |-
      {
        "phone": 99,
        "password": 99
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
