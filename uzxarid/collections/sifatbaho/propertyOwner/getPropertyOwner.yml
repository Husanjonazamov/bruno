info:
  name: getPropertyOwner
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/property-owner/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
