info:
  name: getList
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/auto-evaluation/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
